# digthep-website

Öffentliche Funnel-Seite für digthep (getrennt vom Produkt-Repo `digthep-compliance-kits`).

Aufbau:
- `index.html` – Einseiter: SEO-Artikel, Praxis-Check (reine Filter-Logik, keine Rechtsberatung), Lead-Magnet-Formular, Link zum bezahlten Bündel.

Offene TODOs:
- MailerLite-Formular-Embed in `#leadmagnet` einsetzen (Account bereits angelegt).
- Gumroad-Produktlink für Kit 1 in `#gumroad-link` einsetzen, sobald das Bundle live ist.
- Deployment über Cloudflare Pages, verbunden mit diesem Repo.

Governance: siehe internes Backlog (digthep, DT-Kürzel).
